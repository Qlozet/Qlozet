const VerticalBar = ({ name, per, color }) => {
  return (
    <div className="h-full flex items-end">
      <div className="h-[30%] bg-primary-200 rounded-t-[36px] w-[36px]"></div>
    </div>
  );
};

export default VerticalBar;
