/**
 * Temporary feature flags.
 *
 * Flip to `false` to restore normal routing, or delete this file together with
 * the code paths that reference it once all the pending pages are ready.
 *
 * Updated: 2026-06-04 — Flag disabled to enable full app routing.
 */
export const PAGES_IN_PROGRESS: boolean = false;
