'use client';

import React from 'react';
import { HelpCenterTemplate } from '@/pattern/help-center/help-center-template';

const HelpCenter: React.FC = () => {
  return <HelpCenterTemplate />;
};

export default HelpCenter;
