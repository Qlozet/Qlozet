// Customers Page - Organisms
// Complex components composed of molecules and atoms

// Customer-specific organisms
export { default as CustomerTable } from "@/components/Customer/CustomerTable";

// Customer Metrics Section Organism
export const CustomerMetricsSection = () => {
  // This would contain the composition of multiple DashboardTopCard molecules
  // showing customer-specific metrics
};

// Customer Details Modal Organism  
export const CustomerDetailsModal = () => {
  // This would contain the Modal with CustomerDetails content
};

// Customer History Modal Organism
export const CustomerHistoryModal = () => {
  // This would contain the Modal with OrderHistory content  
};