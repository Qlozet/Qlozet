// Orders Page - Organisms
// Complex components composed of molecules and atoms

// Orders-specific organisms  
export { default as OrderTable } from "@/components/order/OrderTable";

// Order Metrics Section Organism
export const OrderMetricsSection = () => {
  // This would contain the composition of multiple DashboardTopCard molecules
  // showing order-specific metrics (total orders, shipped, pending, returns)
};

// Order Management Modal Organism
export const OrderManagementModal = () => {
  // This would contain modals for order management actions
};